# 調・移：敬拜樂譜移調室

## 在這台 Mac 使用

雙擊本資料夾的 **啟動樂譜工具.command**，再開啟 http://127.0.0.1:8765 。啟動視窗保持開啟即可；關閉後辨識器會停止。如果網頁比辨識器先開，重新整理一次即可。

1. 點「載入測試譜」，或上傳一頁 PDF、JPG、PNG。
2. 選擇原調與目標調。旋律、調號、和弦及斜線和弦的低音一起變更。
3. 用「原始掃描」與「逐小節核對與修正」比對。可修正音高、時值、歌詞、和弦、延音線及反覆。漏掉小節或音符時，可在進階資料編輯中補入。
4. 勾選「已對照原稿核對樂譜」，下載 PDF。MusicXML 可交给 MuseScore 等軟體繼續編輯。

## 本次測試譜

《在主榮耀的同在中》原稿為 A 大調、4/4、7 行、27 小節。初次 Audiveris 辨識僅輸出 26 小節，漏掉第 8 小節全音符；文字辨識亦有大量錯字、漏和弦。工具預設載入的是 **經原圖核對的校正版**，不是重新辨識的結果。

上傳相同 PDF 時，使用 SHA-256 檢查檔案內容後載入此校正版；不靠檔名比對。其他檔案由本機 Audiveris 重新辨識，結果一定標示未校對。

原譜的八分音符橫杠分組會保留。調號已有的升降記號不重複顯示；必要的臨時記號與還原記號依小節狀態顯示。這是工具後續版本的固定原則。

## 使用額度與隱私

正常使用不呼叫 OpenAI／ChatGPT 或其他大型語言模型 API，因此別人使用不會消耗開發者的 ChatGPT token。轉調、排版、PDF 匯出在使用者瀏覽器執行；辨識在使用者本機執行。檔案不會送到雲端辨識服務。辨識暫存檔完成後刪除。

網站初次載入及初次下載套件需要網路流量；本機版本備妥後，日常使用可離線。若未來改為多人共用伺服器，會需要主機資源，應另行評估部署，不代表一定需要 AI API。

## 第一版範圍與限制

- 多頁 PDF、單張 JPG/PNG；單一旋律聲部、高音譜號為最穩定；檔案最多 15 MB。
- 不支援鋼琴多聲部、倚音、複雜連音群或可靠的手寫譜辨識。
- 自動辨識的歌詞及和弦仍須人工校正；不能由本頁校正版推論其他譜的辨識準確率。
- 預覽與 PDF 保留音樂結構並重新排版，不承諾逐像素還原掃描版。PDF 是高解析度影像；MusicXML 保留可編輯音樂資料。
- 本機辨識器目前隨附 macOS Apple Silicon 版本（本台電腦）。Windows、Intel Mac 或手機需另行配置對應引擎；不能把此資料夾直接當成跨平台安裝包。
- 私人網站可直接試用校正版。其他樂譜請優先從本機網址上傳；部分瀏覽器會限制私人網站連接本機服務。

## 開發

固定需求在 AGENTS.md。前端使用 React / Vinext、abcjs、PDF.js、jsPDF。local/bridge.py 使用 Python 標準函式庫與 Pillow（調整照片方向及辨識尺寸），僅綁定 127.0.0.1；啟動 Audiveris 5.11.0。local/engine 中的引擎不會被提交到私人網站原始碼。

套件安裝：pnpm install。建置：pnpm build。型別检查：pnpm exec tsc --noEmit。

音樂測試涵蓋 14 調、129 個音高、135 個音符／休止符事件、27 小節、旋律往返移調、斜線和弦、節奏與歌詞不變、MusicXML 往返、橫杠以及調號／臨時記號。

第三方授權：Audiveris 為 AGPL-3.0，Tesseract 資料為 Apache-2.0；abcjs / PDF.js / jsPDF 等依各套件授權。若再散佈本機引擎，需附原作者授權與對應來源。本資料夾的引擎源自 https://github.com/Audiveris/audiveris/releases/tag/5.11.0 ，對應來源 https://github.com/Audiveris/audiveris/tree/5.11.0 。樂譜版權歸原權利人。
